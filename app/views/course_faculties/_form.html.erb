<%= form_for(course_faculty) do |f| %>
  <% if course_faculty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_faculty.errors.count, "error") %> prohibited this course_faculty from being saved:</h2>

      <ul>
      <% course_faculty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.collection_select :course_id, Course.all, :id, :listing, selected: :course_id %>
  </div>

  <div class="field">
    <%= f.label :professor_id %>
    <%= f.collection_select :professor_id, Professor.all, :id, :fullname, selected: :professor_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
